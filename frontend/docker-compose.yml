version: '3.7'

services:
  frontend:    
    build:      
      context: .
      dockerfile: Dockerfile
    image: "frontend:${TAG}"
    container_name: container-frontend
    restart: always    
    ports:
      - "3000:3000"
      
       